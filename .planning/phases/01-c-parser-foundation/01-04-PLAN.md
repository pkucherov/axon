---
phase: 01-c-parser-foundation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/axon/core/parsers/csharp_lang.py
  - tests/core/test_parser_csharp.py
autonomous: true
requirements: [CS-02]
gap_closure: true

must_haves:
  truths:
    - "Method, constructor, and property nodes parsed inside a namespace block have cs_namespace set in their properties dict"
    - "All 29+ C# tests pass with no regressions"
  artifacts:
    - path: "src/axon/core/parsers/csharp_lang.py"
      provides: "cs_namespace propagation in _extract_method, _extract_constructor, _extract_property"
      contains: "cs_namespace"
    - path: "tests/core/test_parser_csharp.py"
      provides: "Test coverage for namespace propagation on method/constructor/property nodes"
  key_links:
    - from: "src/axon/core/parsers/csharp_lang.py"
      to: "SymbolInfo.properties"
      via: "props['cs_namespace'] = self._current_namespace"
      pattern: "cs_namespace.*_current_namespace"
---

<objective>
Close UAT gap #6: method, constructor, and property nodes inside a namespace block do not receive cs_namespace in their properties dict.

Purpose: The UAT reported that only class-level nodes propagate cs_namespace. Method, constructor, and property nodes emitted from within a namespace block must also carry cs_namespace so the full symbol graph is namespace-aware.

Output: Patched csharp_lang.py and a new test verifying the fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/axon/core/parsers/csharp_lang.py
@tests/core/test_parser_csharp.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Propagate cs_namespace in _extract_method, _extract_constructor, _extract_property</name>
  <files>src/axon/core/parsers/csharp_lang.py, tests/core/test_parser_csharp.py</files>
  <behavior>
    - Test: parsing `namespace MyApp.Services { class Foo { void Bar() {} } }` yields a method SymbolInfo for Bar where `properties['cs_namespace'] == 'MyApp.Services'`
    - Test: parsing a namespace with a constructor `Foo() {}` yields a constructor SymbolInfo with `properties['cs_namespace'] == 'MyApp.Services'`
    - Test: parsing a namespace with a property `public int Age { get; set; }` yields a property SymbolInfo with `properties['cs_namespace'] == 'MyApp.Services'`
    - Test: symbols outside any namespace block still have no cs_namespace key in properties (no regression)
    - Test: class nodes within a namespace still have cs_namespace (no regression on existing behavior)
  </behavior>
  <action>
Write the failing tests first in a new test class `TestNamespacePropagation` in `tests/core/test_parser_csharp.py`. Run them to confirm RED.

Then fix `src/axon/core/parsers/csharp_lang.py` — in all three methods, build a `props` dict and add `cs_namespace` following the identical pattern already used in `_extract_class`, `_extract_interface`, and `_extract_enum`:

In `_extract_method` (around line 225): after the existing symbol construction, add:
```python
props: dict = {}
if self._current_namespace:
    props["cs_namespace"] = self._current_namespace
```
then pass `properties=props` in the `SymbolInfo(...)` call (or merge into existing props dict if one already exists).

In `_extract_constructor` (around line 260): same pattern — build `props` dict with `cs_namespace` if `self._current_namespace` is set; pass to `SymbolInfo`.

In `_extract_property` (around line 284): same pattern.

Do NOT change the method signatures, return types, or any other behavior. The fix is strictly additive — adding the `cs_namespace` key when `self._current_namespace` is non-empty. Mirror the exact guard `if self._current_namespace:` used in `_extract_class` and `_extract_enum`.

Read the full method bodies before editing to verify where SymbolInfo is constructed in each method and whether a `props` dict already exists.
  </action>
  <verify>
    <automated>cd H:/Github/axon && uv run --with pytest --with pytest-asyncio python -m pytest tests/core/test_parser_csharp.py -q 2>&1 | tail -5</automated>
  </verify>
  <done>All C# parser tests pass (30+ tests, 0 failed). New TestNamespacePropagation class contains at least 5 passing assertions covering method, constructor, property, no-namespace, and class regression cases.</done>
</task>

<task type="auto">
  <name>Task 2: Ruff lint check and full core suite</name>
  <files></files>
  <action>
Run ruff on the changed files to catch any formatting or import issues introduced by the fix:

```
uv run ruff check src/axon/core/parsers/csharp_lang.py tests/core/test_parser_csharp.py
uv run ruff format --check src/axon/core/parsers/csharp_lang.py tests/core/test_parser_csharp.py
```

If ruff reports issues, fix them (typically trailing whitespace or import order). Then run the full core test suite to confirm no regressions on Python/TypeScript parsers or other modules.
  </action>
  <verify>
    <automated>cd H:/Github/axon && uv run ruff check src/axon/core/parsers/csharp_lang.py tests/core/test_parser_csharp.py && uv run --with pytest --with pytest-asyncio python -m pytest tests/core/ -q 2>&1 | tail -5</automated>
  </verify>
  <done>Ruff reports 0 errors. Full core suite passes with 0 failures (698+ tests). No regression on Python/TypeScript parsers.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `uv run --with pytest --with pytest-asyncio python -m pytest tests/core/test_parser_csharp.py -v` shows all tests green including TestNamespacePropagation
2. `uv run ruff check src/axon/core/parsers/csharp_lang.py` exits 0
3. `uv run --with pytest --with pytest-asyncio python -m pytest tests/core/ -q` shows 0 failures
4. Grep confirms three new `cs_namespace` propagation blocks in csharp_lang.py (one each in _extract_method, _extract_constructor, _extract_property)
</verification>

<success_criteria>
- `symbol.properties['cs_namespace']` is correctly set for method, constructor, and property SymbolInfo nodes inside namespace blocks
- UAT test 6 ("Namespace Metadata in Properties") would now pass: method Bar inside namespace MyApp.Services has cs_namespace == 'MyApp.Services'
- Full test suite green, ruff clean
</success_criteria>

<output>
After completion, create `.planning/phases/01-c-parser-foundation/01-04-SUMMARY.md`
</output>
